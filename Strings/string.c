#include <stdio.h>

int main(int argc, char const *argv[])
{
    char name[10] = {'J', 'o', 'h', 'n'};
    printf("%s\n", name);

    char name2[10];
    scanf("%s\n", name2);
    // printf("%s\n", name2);
    return 0;
}
